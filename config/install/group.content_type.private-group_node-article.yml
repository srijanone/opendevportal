langcode: en
status: true
dependencies:
  config:
    - group.type.private
    - node.type.article
  module:
    - gnode
    - node
id: private-group_node-article
label: 'Private: Group node (Blog)'
description: 'Adds <em class="placeholder">Blog</em> content to groups both publicly and privately.'
group_type: private
content_plugin: 'group_node:article'
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: false
