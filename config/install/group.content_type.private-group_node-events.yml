langcode: en
status: true
dependencies:
  config:
    - group.type.private
    - node.type.events
  module:
    - gnode
    - node
id: private-group_node-events
label: 'Private: Group node (Event)'
description: 'Adds <em class="placeholder">Event</em> content to groups both publicly and privately.'
group_type: private
content_plugin: 'group_node:events'
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: false
