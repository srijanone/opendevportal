langcode: en
status: true
dependencies:
  config:
    - group.type.private
    - node.type.listing_pages
  module:
    - gnode
    - node
id: private-group_node-listing_pages
label: 'Private: Group node (Landing page)'
description: 'Adds <em class="placeholder">Landing page</em> content to groups both publicly and privately.'
group_type: private
content_plugin: 'group_node:listing_pages'
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: false
