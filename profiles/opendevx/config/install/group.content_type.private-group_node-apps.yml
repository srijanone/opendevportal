langcode: en
status: true
dependencies:
  config:
    - group.type.private
    - node.type.apps
  module:
    - gnode
    - node
id: private-group_node-apps
label: 'Private: Group node (Application)'
description: 'Adds <em class="placeholder">Application</em> content to groups both publicly and privately.'
group_type: private
content_plugin: 'group_node:apps'
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: false
